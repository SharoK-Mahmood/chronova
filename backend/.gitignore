node_modules
dist
.env
*.db
*.db-journal
# Keep seed product images in git (needed for Render deploys).
# Ignore only local scratch folders under uploads.
uploads/products/unsorted/**
uploads/products/**/.DS_Store
